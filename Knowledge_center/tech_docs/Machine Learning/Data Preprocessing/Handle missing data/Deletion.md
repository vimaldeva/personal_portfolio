Deletion:
Listwise Deletion: Remove entire rows that contain any missing values. Use this only if you have a very large dataset and the missing values are few.
Pairwise Deletion/Dropping Columns: Remove entire columns (features) if they have a high percentage of missing values.